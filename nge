arr = [6,0,8,1,3]
ans = [] 
for i in range(0,len(arr)):
    flag = True 
    for j in range(i+1,len(arr)):
        if(arr[j]>arr[i]):
            ans.append(arr[j]) 
            flag = False 
            break 
    if(flag == True):
        ans.append(-1) 
print(ans) 
